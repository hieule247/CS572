{
  "name": "add_number",
  "version": "1.0.0",
  "description": "This is an application to demonstrate add two number: one number is given by path parameter and second number as a query string",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Add",
    "Number"
  ],
  "author": "Hieu Le",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
